# ─── Provenant Production Environment Variables ───────────────────────────────
# Copy this file to .env and fill in the values.
# NEVER commit .env to source control.

# ─── Core ─────────────────────────────────────────────────────────────────────

# Node environment (leave as production)
NODE_ENV=production

# API server port
PORT=4000

# API public URL (used for OAuth callbacks, email links)
APP_URL=https://api.yourdomain.com

# Frontend origin (for CORS)
CORS_ORIGIN=https://app.yourdomain.com

# Log level: error | warn | info | debug
LOG_LEVEL=info

# ─── Database ─────────────────────────────────────────────────────────────────

# PostgreSQL connection string
DATABASE_URL=postgresql://provenant:CHANGE_ME@postgres:5432/provenant

# PostgreSQL password (used by docker-compose)
POSTGRES_PASSWORD=CHANGE_ME

# ─── Redis ────────────────────────────────────────────────────────────────────

# Redis connection URL (optional — enables Bull job queues)
# If not set, eval runs and webhooks execute synchronously in-process
REDIS_URL=redis://redis:6379

# ─── Auth ─────────────────────────────────────────────────────────────────────

# JWT signing secret — generate with: openssl rand -hex 32
JWT_SECRET=CHANGE_ME_USE_openssl_rand_hex_32

# ─── GitHub OAuth (optional) ──────────────────────────────────────────────────
# Create an OAuth App at: https://github.com/settings/applications/new
# Authorization callback URL: https://api.yourdomain.com/api/auth/github/callback

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ─── Anthropic AI (for eval execution and AI case generation) ─────────────────
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-CHANGE_ME

# ─── Email / SMTP (optional) ──────────────────────────────────────────────────
# If not configured, invitation links and password resets are logged to console.
# Supported: any SMTP server (SendGrid, Mailgun, Postmark, AWS SES, etc.)

SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=SG.CHANGE_ME
SMTP_FROM=Provenant <noreply@yourdomain.com>
